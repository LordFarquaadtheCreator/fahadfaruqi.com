# src/lib/data/

Static data assets.

- **portfolio.json** — Source of truth for all portfolio content. Structured: `profile`, `contact`, `home`, `resume` (experiences, education, projects, toolkit), `blog`. Consumed by `src/lib/model/data.ts`.
